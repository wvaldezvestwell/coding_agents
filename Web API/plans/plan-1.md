# Implementation Plan — 1: Add GET endpoint to retrieve the list of USA states
> Auto-generated by /dev-start. Review before execution.

## Summary

Add a `GET /states` endpoint to the Web API project that returns the list of the 50 USA states as a JSON array, so client applications can populate state selectors and validate state input without maintaining their own copy of the list. The data is a hardcoded in-memory collection — no database, configuration, or external service. The endpoint follows the existing minimal-API style used by `/weatherforecast` in `Program.cs`.

## Scope

**In scope**
- `GET /states` endpoint in the Web API project (minimal API style, registered in `Program.cs`).
- A `State` record with `Code` (2-letter uppercase USPS abbreviation) and `Name`.
- All 50 states returned as a JSON array, sorted alphabetically by `Name`.
- `.WithName("GetStates")` so the endpoint appears in the OpenAPI document.
- A static class (e.g. `UsaStates`) holding the hardcoded list, to keep `Program.cs` readable.

**Out of scope**
- Database or any persistence layer.
- Territories (PR, GU, etc.) and DC.
- Filtering, paging, or search.
- Authentication/authorization.
- New NuGet packages.

## Acceptance Criteria

- [ ] `GET /states` returns HTTP 200 OK with content type `application/json`.
- [ ] The response body is an array of exactly 50 objects, each with `code` and `name` properties.
- [ ] State codes are the official 2-letter USPS abbreviations in uppercase.
- [ ] The list is sorted alphabetically by state name.
- [ ] The data is hardcoded in the application — no database, configuration file, or external API call.
- [ ] The endpoint appears in the OpenAPI document exposed in Development (`/openapi/v1.json`).
- [ ] (Implied) JSON property names are camelCase (`code`, `name`) — the ASP.NET Core default serializer already does this for records.
- [ ] (Implied) Existing `/weatherforecast` endpoint continues to work unchanged.

## Implementation Changes

| File / Component | Change Type | Notes |
|---|---|---|
| `Web API/Program.cs` | Modify | Map `GET /states` returning the static list, `.WithName("GetStates")`, placed alongside `/weatherforecast`. |
| `Web API/UsaStates.cs` | Add | Static class with the `State` record (`Code`, `Name`) and a readonly array of the 50 states pre-sorted by name. |
| `Web API/Web API.http` | Modify | Add a `GET {{host}}/states` request for manual testing, matching the existing file's style. |

## Testing Strategy

**Manual**
1. `dotnet run` in Development, then `GET /states` (via `Web API.http` or curl).
2. Verify 200 OK, `application/json`, 50 items, first item `{ "code": "AL", "name": "Alabama" }`, last `{ "code": "WY", "name": "Wyoming" }`.
3. Fetch `/openapi/v1.json` and confirm an operation named `GetStates` on path `/states`.
4. Hit `/weatherforecast` to confirm no regression.

**Automated**
- The solution currently has no test project. Adding one (xUnit + `Microsoft.AspNetCore.Mvc.Testing`) would require new packages, which the ticket rules out ("No new NuGet packages needed") — so validation is manual for this ticket. Unit test scenarios are listed below in case a test project is added later.

**Environments**
- Local Development only (OpenAPI is only mapped in Development).

## Unit Tests to Add

| Scenario | Expected Result | Priority |
|---|---|---|
| GET /states status and content type | 200 OK, `application/json` | High |
| Response contains exactly 50 states | Array length == 50 | High |
| All codes are 2 uppercase letters and unique | Regex `^[A-Z]{2}$`, 50 distinct codes | High |
| List sorted alphabetically by name | Sequence equals itself ordered by `Name` | Medium |
| Spot-check known entries | Contains `AL/Alabama`, `CA/California`, `WY/Wyoming`; excludes `DC` and `PR` | Medium |
| OpenAPI document lists GetStates | `/openapi/v1.json` includes `/states` | Low |

## Open Questions & Risks

| # | Question / Risk | Owner | Status |
|---|---|---|---|
| 1 | No test project exists and the ticket says no new NuGet packages — automated tests deferred; is manual verification acceptable? | Walterio | Open (assumed yes) |
| 2 | Data correctness risk: a typo in one of the 50 hardcoded entries. Mitigate by spot-checking against the official USPS list during review. | Claude | Mitigated |
| 3 | Sort semantics: plain ordinal sort by name is sufficient for English state names (no accents/case collisions). | Claude | Closed |

## Rollback Plan

No side effects (no migrations, config, or infrastructure changes) — revert the commit(s) on this branch, or delete the branch before merge.

## Notes

- Branch: `fix/1-get-usa-states-endpoint` off `main` (work item type Issue → `fix` prefix per ticket-manager config).
- The `State` record serializes to camelCase (`code`, `name`) by default in minimal APIs, matching the example payload in the ticket.
- The state list lives in its own static class rather than inline in `Program.cs`, per the ticket's technical note ("optionally move the state list into its own static class").
- Work item: https://dev.azure.com/enroutesystemsazure/Agents%20AI/_workitems/edit/1
